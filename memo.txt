ToDo:

* warn parse error
  * when the parse doesn't complete. tell the user the current line number and possible cause of error.

* optimization
  * [1, 2, 3].atPut(3, 4) -> [1, 2, 3][3] = 4
  * (1 < 3).ifTrue(function(){return 'yay';}) -> if (1 < 3) { return 'yay';  }
  * etc

* command-line tool
  * compile file, compile and print, interactive shell

* interactive web interface
  * something like "try coffeescript"
